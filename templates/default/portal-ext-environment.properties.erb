#
# Disable google ping to keep sites off the radar by default.
#
blogs.ping.google.enabled=false

#
# For web service access
#
axis.servlets.hosts.allowed=127.0.0.1,SERVER_IP,localhost
axis.servlet.https.required=false

tunnel.servlet.hosts.allowed=127.0.0.1,SERVER_IP,localhost
tunnel.servlet.https.required=false